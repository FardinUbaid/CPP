#include<iostream>
using namespace std;

int main(){

    //String = Is a data type of made of multiple charater datatypes(values).

    //string name = "Kousik";

    //cout<<name[3]; // single characters can be accessed from the string using square brackets with index.

    //Basic Loop



    /*
        for(varible to iterate with ; condition ; increment / decrement){
        
            //code to be executed

        }
    
    */

   /*string name = "Muhtasib Ibtida Kousik";


   for(int i = 0; i < 1 ;i++){

        cout<< "Index["<<i<<"]="<<name[i]<<'\n';

   }
   */
    /*for(int i=0;i<10;i++){

        if(i==5){
            continue;
        }else{
            cout<<i;
        }
    }
    */

   //MARK:String Methods

    string name="MuhtasIb Ibtida Kousik";
   
    //int length = name.length();
    //cout<<length; //outputs the length of the variable;

    //cout<<name.empty();//returns 0 if empty and 1 if not empty

    //cout<<name.append(" Bodda");//Add the given string or char at the end of the string value.

    //cout<<name.at(10);//returns the char at the given index

    //cout<<name.insert(5,"ABCD");//Inserts the given string at the given index

    //cout<<name.find('I');//returns the index number of the given char

    //cout<<name.erase(0,8);//returns the erased value of the variable from given index to end index

    //string subname=name.substr(9,6);//crops the string from given index to the number of chars

    //cout<<name;
    //cout<<"\n"<<subname;

    return 0;
}